image = "img.png"
rotate image by -180
subimg = crop (2, 2, 10, 10) from image
resize image to (100, 100)
resize image by 0.5
flip image vertically
modify image contrast by 5
for all img in dir {
    red = img(R) / 2
    save red as "lala.png"
}
print 0.5
